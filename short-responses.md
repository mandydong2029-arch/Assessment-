1. If we are collaborating with a classmate, the first thing each teammate needes to do is create there own branches to work at. It's important to do this step because you need to merge the branches together at the end.
2. developers use branches to separate their work from their teammates work and merge together at the end.
3. Git is a Command Line tool that allows you to track and manage changes to your code. Github is Cloud based service that allows developers to store, track, manage and control changes to their code. They work together by; git is the command line tool used in the platform github to create commands.
4. The following sequence is wrong because git add . is suppose to go first and then the git commit -m and lastly git push
